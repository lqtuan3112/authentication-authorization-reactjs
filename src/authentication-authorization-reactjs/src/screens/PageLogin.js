import React from 'react';
import PropTypes from 'prop-types';

PageLogin.propTypes = {};

function PageLogin(props) {
    return (
        <div>
            <button>Login with admin</button>
            <button>Login with user</button>
        </div>
    );
}

export default PageLogin;
